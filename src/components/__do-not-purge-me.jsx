/**
 * This component is not in use, it just a hack to make sure some classes are in the whitelist of purge css.
 *
 * @constructor
 */
export function DoNotPurgeMe() {
  return <div className="aspect-w-2 aspect-h-1" />
}
