export default class Image {
  constructor({ srcSet, images, src, width, height }) {
    this.srcSet = srcSet
    this.images = images
    this.src = src
    this.width = width
    this.height = height
  }
}
